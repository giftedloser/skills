interface:
  display_name: "Check Diff"
  short_description: "Audit-only review of defects introduced by a Git diff."
  default_prompt: "Use $check-diff to audit this branch or working-tree change and produce a focused remediation prompt for a separate correction session."

policy:
  allow_implicit_invocation: false
