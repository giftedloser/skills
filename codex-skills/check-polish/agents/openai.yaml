interface:
  display_name: Check Polish
  short_description: Audit-only. UI/UX, responsiveness, accessibility, and interaction quality by running the interface. NOT APPLICABLE for non-UI repos.
  default_prompt: Use $check-polish to audit UI, UX, accessibility, and interaction quality.

policy:
  allow_implicit_invocation: false
