interface:
  display_name: "New Idea"
  short_description: "Pressure-test an idea before project setup"
  default_prompt: "Use $new-idea to challenge this idea and produce an honest project handoff."

policy:
  allow_implicit_invocation: true
