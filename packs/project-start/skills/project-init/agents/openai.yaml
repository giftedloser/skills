interface:
  display_name: "Project Init"
  short_description: "Create or normalize a project safely"
  default_prompt: "Use $project-init to scaffold a new project or normalize this repository safely."

policy:
  allow_implicit_invocation: true
