interface:
  display_name: "Check Work"
  short_description: "Audit requested workflows end to end"
  default_prompt: "Use $check-work to run and verify the explicitly requested user workflows end to end."

policy:
  allow_implicit_invocation: true
