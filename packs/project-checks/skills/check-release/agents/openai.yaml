interface:
  display_name: "Check Release"
  short_description: "Audit one candidate for a release decision"
  default_prompt: "Use $check-release to audit one identified release candidate and return the release decision."

policy:
  allow_implicit_invocation: true
