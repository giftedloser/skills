interface:
  display_name: "Check Code"
  short_description: "Audit focused implementation correctness"
  default_prompt: "Use $check-code to audit a declared module or the three most critical paths."

policy:
  allow_implicit_invocation: true
