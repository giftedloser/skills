interface:
  display_name: Check Code
  short_description: Audit-only. Implementation soundness - correctness, reliability, data integrity, code quality, no slop.
  default_prompt: Use $check-code to audit implementation correctness, reliability, and production readiness.

policy:
  allow_implicit_invocation: false
