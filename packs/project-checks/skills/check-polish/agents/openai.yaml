interface:
  display_name: "Check Polish"
  short_description: "Audit UI quality through real interaction"
  default_prompt: "Use $check-polish to audit this existing interface for UI, UX, accessibility, and interaction defects."

policy:
  allow_implicit_invocation: true
