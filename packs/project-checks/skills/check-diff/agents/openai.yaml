interface:
  display_name: "Check Diff"
  short_description: "Audit defects introduced by a selected diff"
  default_prompt: "Use $check-diff to audit this branch or working-tree change for change-caused defects."

policy:
  allow_implicit_invocation: true
