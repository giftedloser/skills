interface:
  display_name: "Check Performance"
  short_description: "Measure one workflow for real bottlenecks"
  default_prompt: "Use $check-performance to measure one named workflow and identify its actual bottleneck."

policy:
  allow_implicit_invocation: true
