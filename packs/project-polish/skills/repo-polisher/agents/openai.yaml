interface:
  display_name: "Repo Polisher"
  short_description: "Cosmetic repository polish — identity, README craft, diagrams, templates, and metadata, evidence-backed and per-project."
  default_prompt: "Use $repo-polisher to elevate this repository's presentation and identity without changing source behavior."

policy:
  allow_implicit_invocation: true
